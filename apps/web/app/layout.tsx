export const metadata = { title: 'TheLux Vibe Builder', description: 'MCP + AI micro‑SaaS' };
export default function RootLayout({ children }) {
  return <html lang="en"><body style={{fontFamily:'system-ui',margin:0}}>{children}</body></html>;
}
